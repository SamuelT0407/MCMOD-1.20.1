# ⛽ createdieselgenerators-server.toml
## Create Diesel Generators（柴油发电机）- 服务器配置翻译

---

```toml
["Server Configs"]  # 服务器配置

    # 油桶的最大宽度
    # 油桶是多方块结构，这限制了它的最大尺寸
    "Max Oil Barrel Width" = 3

    # 罐子是否可以用液体喷嘴填充
    # 液体喷嘴是Create的方块，可以向容器注入流体
    "Canister can be filled by spouts" = true

    # ⚠️ 可燃物着火时是否会爆炸
    # true = 柴油、汽油等可燃流体遇火会爆炸
    # 设为false可以提高安全性
    "Combustibles blow up" = true

["Server Configs"."Diesel Engines"]  # 柴油引擎设置

    # 涡轮增压柴油引擎的速度倍率
    # 涡轮增压版比普通版更快
    "Turbocharged Diesel Engine Speed Multiplier" = 2.0

    # 涡轮增压柴油引擎的燃料消耗倍率
    # 1.0 = 正常消耗，2.0 = 两倍消耗
    "Turbocharged Diesel Engine Burn Rate Multiplier" = 1.0

    # 是否启用普通柴油引擎
    "Normal Diesel Engines" = true

    # 是否启用模块化柴油引擎
    # 模块化引擎可以拼接成更大的引擎组
    "Modular Diesel Engines" = true

    # 是否启用巨型柴油引擎
    # 巨型引擎输出功率最高
    "Huge Diesel Engines" = true

    # 是否可以用物品（桶）填充柴油引擎
    # false = 只能用管道输入燃料
    "Engines filled with a bucket" = false

    # 是否可以用红石禁用柴油引擎
    # true = 红石信号可以关闭引擎
    "Engines disabled with redstone" = true

["Server Configs"."Oil Config"]  # 石油配置

    # 无限石油区块阈值
    # 石油量超过此值的区块视为无限石油区块
    "Infinite oil chunk threshold" = 10000000

    # 石油区块阈值
    # 石油量超过此值才算"石油区块"
    "Oil chunk threshold" = 4000000

    # 普通石油区块的石油量倍率
    "Normal oil chunks oil amount multiplier" = 1.3

    # 高产石油区块的石油量倍率
    "High oil chunks oil amount multiplier" = 2.0

    # 石油区块地图缩放比例
    # 影响石油扫描仪显示的地图
    "Oil chunk map scale" = 1.0

    # 石油扫描仪的最大等级
    "Max Oil Scanner Level" = 10000
```

---

## 📝 配置说明

| 配置项 | 当前值 | 说明 |
|-------|-------|------|
| Combustibles blow up | true | ⚠️ 燃料会爆炸，注意安全！ |
| Turbocharged Speed | 2.0 | 涡轮增压版速度翻倍 |
| Normal Diesel Engines | true | 普通柴油引擎可用 |
| Huge Diesel Engines | true | 巨型柴油引擎可用 |

**柴油引擎类型**:
1. **普通柴油引擎** - 基础款
2. **模块化柴油引擎** - 可以拼接组合
3. **巨型柴油引擎** - 功率最高
4. **涡轮增压版** - 速度翻倍

**安全提示**: 如果不想燃料爆炸，设置 `"Combustibles blow up" = false`
