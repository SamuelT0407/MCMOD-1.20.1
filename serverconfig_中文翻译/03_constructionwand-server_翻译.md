# 🔨 constructionwand-server.toml
## 建造魔杖 - 服务器配置翻译

---

```toml
# 这是建造魔杖的服务器配置。
# 服务器配置是针对每个存档的，位于 /saves/存档名/serverconfig 文件夹。
# 如果想为所有新存档使用相同配置，请复制到 /defaultconfigs 文件夹。

[stone_wand]  # 石质魔杖（最基础的魔杖）

    # 魔杖耐久度
    # 范围: ≥ 1
    durability = 131

    # 魔杖方块限制（一次最多放置多少个方块）
    # 范围: ≥ 1
    limit = 9

    # 使用天使核心时的最大放置距离（方块）
    # 天使核心允许在空中放置方块
    # 0 表示禁用天使核心功能
    # 范围: ≥ 0
    angel = 0

    # 破坏核心的方块限制
    # 破坏核心允许快速拆除方块
    # 0 表示禁用破坏核心功能
    # 范围: ≥ 0
    destruction = 0

    # 是否允许通过合成升级魔杖
    # 将魔杖与核心放在一起合成可以添加功能
    upgradeable = false

[iron_wand]  # 铁质魔杖

    durability = 250      # 耐久度
    limit = 27            # 一次放置27个方块
    angel = 2             # 天使核心距离2格
    destruction = 9       # 破坏核心限制9个方块
    upgradeable = true    # 可升级

[diamond_wand]  # 钻石魔杖

    durability = 1561     # 高耐久度
    limit = 128           # 一次放置128个方块
    angel = 8             # 天使核心距离8格
    destruction = 25      # 破坏核心限制25个方块
    upgradeable = true

[infinity_wand]  # 无尽魔杖（无耐久限制）

    limit = 1024          # 一次放置1024个方块！
    angel = 16            # 天使核心距离16格
    destruction = 81      # 破坏核心限制81个方块
    upgradeable = true

[misc]  # 杂项设置

    # 创造模式下无尽魔杖的方块限制
    # 范围: ≥ 1
    InfinityWandCreative = 2048

    # 最大放置范围（方块）
    # 0 表示无限制。用于防止服务器卡顿
    # 范围: ≥ 0
    MaxRange = 100

    # 可以撤销的操作数量
    # 范围: ≥ 0
    UndoHistory = 3

    # 持有天使核心时，如果下落超过10格，是否在脚下放置方块
    # 可用于防止跌落死亡或掉入虚空
    AngelFalling = false

    # 在"相似模式"下视为相同的方块列表
    # 用分号(;)分隔不同的方块ID
    # 这些方块在放置时可以互相替代
    SimilarBlocks = ["minecraft:dirt;minecraft:grass_block;minecraft:coarse_dirt;minecraft:podzol;minecraft:mycelium;minecraft:farmland;minecraft:dirt_path;minecraft:rooted_dirt"]

[tileentity]  # 方块实体设置

    # 方块实体的名单列表
    # 可以添加方块ID如 minecraft:chest 或模组ID如 minecraft
    TEList = ["chiselsandbits"]

    # 名单模式
    # true = 白名单模式（只允许列表中的方块实体被放置）
    # false = 黑名单模式（禁止列表中的方块实体被放置）
    TEWhitelist = false
```

---

## 📝 配置说明

| 魔杖类型 | 耐久 | 方块限制 | 天使距离 | 破坏限制 |
|---------|------|---------|---------|---------|
| 石质 | 131 | 9 | 0 | 0 |
| 铁质 | 250 | 27 | 2 | 9 |
| 钻石 | 1561 | 128 | 8 | 25 |
| 无尽 | ∞ | 1024 | 16 | 81 |

**提示**: 如果觉得石质魔杖太弱，可以增加 limit 值！
